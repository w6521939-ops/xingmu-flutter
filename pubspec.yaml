name: xingmu_ai_video_studio
description: "Flutter Android and HarmonyOS client for AI motion-comic video generation."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/branding/app_icon.png
    - assets/showcase/moon-courier-hero.png
