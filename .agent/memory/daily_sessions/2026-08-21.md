# 2026-08-21

## 较早单段视频基线（历史）

- 完成 Flutter 模型与视频实验室、本地 FFmpeg 任务服务和付费模型目录边界。
- `dart format lib test`：passed，42 files、0 changed。
- `flutter analyze`：passed，0 issues。
- `flutter test`：passed，122 tests。
- Python/FFmpeg tests：passed，8 tests。
- Android 15/API 35 x86_64 模拟器：最终 APK 安装、冷启动导航、模型目录、付费入口、真实 4 秒视频生成与 GIF 显示 passed；候选包完成 10 页矩阵。
- HarmonyOS 6.1.1/API 24 x86_64 模拟器：API 20 配置 HAP 构建/安装、9/10 页、模型目录、付费入口、真实生成与 GIF 显示 passed；设置页、最终 MP4 复制反馈 not run。
- API 20 专用镜像、真实手机、DevEco 签名包、千问/Wan 云推理和应用内支付 not run。
- 原始 Windows/Electron 漫剧源码目录未修改；未执行 GitHub 上传。

## 本地三镜头漫剧阶段（历史）

- 将本地实验目标从单段动态字幕卡调整为 3 个镜头、3 段 Windows 中文配音、字幕与 FFmpeg 镜头运动的漫剧模板。
- 固定模板为 `《月背最后一单》`，使用 `assets/showcase/motion_comic/` 下三张 2026-08-21 OpenAI 内置 ImageGen 画面；它们引用项目自有月球快递视觉，不使用 stock/franchise 素材。
- 本地任务必须标记 `executionKind=template`、`visualSource=fixed_project_assets`、`generatedForRequest=false`、`containsAiGeneratedAssets=true`、`assetProvenance=openai_imagegen_project_assets`，说明任意输入不会重新绘制视觉，同时如实披露固定画面为预生成 ImageGen 项目素材。
- 模型目录扩展为文本/图片/视频/声音四组；百炼四类模型仅显示 `requires_configuration`。云 HTTP Adapter 未实现，配置 Key 不能直接启用，App 也不能发起支付。
- OpenAPI 保留 legacy `video-jobs` 并新增 `comic-jobs`、阶段、三镜头、manifest/script 与真实性字段。
- 本轮 Video Lab OpenAPI YAML、本地引用、路径参数与实现字段形状检查：`passed`（12 paths、12 operations、60 refs、0 unresolved；未运行独立语义 linter）。
- 漫剧升级后的 Dart format：`passed`，42 files、0 changed；Flutter analyze：`passed`，0 issues；Flutter tests：`passed`，130/130。
- Python/FFmpeg/Windows TTS 漫剧服务测试：`passed`，19/19；实际成片媒体检查：`passed`，9.041814 秒、720×1280、H.264/yuv420p + AAC 44.1 kHz mono，三镜头与 GIF 均有效。
- Android 15/API 35 x86_64 最终 E2E：`passed`。修复冷启动首帧零宽导致的负卡片约束后，两次冷启动、四类模型、云端禁用边界、本地三镜头 100%、GIF 与 MP4 Range 206 均通过。
- HarmonyOS 6.1.1/API 24 x86_64 最终 E2E：`passed`。API 20 配置的 no-codesign HAP 安装、四类模型、云端禁用边界、本地三镜头 100%、GIF 与 MP4 地址复制均通过；API 20 专用镜像仍为 `not run`。
- 故事长度统一为 4–500 字后重新构建双端当前包并执行差异 smoke：`passed`。Android 与 HarmonyOS 均用 500 字实际生成完成；第 501 字由输入层限制在 `500/500`，服务端无新增 POST。

## Wan hybrid 首尾帧分镜视频

- 新增 `wan_fixed_frames_motion_comic` Pipeline，固定组合为 `local_storyboard_template + fixed_moon_courier_assets + wan2.7-i2v-2026-04-25 + windows_sapi_huihui`。
- 为 E01-SH01 至 E01-SH03 各补一张 ImageGen 尾帧，每镜返回 `firstFrameUrl`、`lastFrameUrl`、`motionPrompt` 和嵌套 `videoTask`。
- hybrid Job/Output 声明 `executionKind=hybrid`、`visualSource=fixed_project_assets`、`generatedForRequest=true`、`containsAiGeneratedAssets=true`、`assetProvenance=openai_imagegen_project_assets` 与 `modelExecution={text: local, image: pre_generated, video: cloud, voice: local}`。
- 成片为 3 段供应商 MP4 标准化后合片，输出 `compositionType=shot_videos_concat`、`sourceClipCount=3`；GIF 只是预览。主 App 增加最终 MP4 内嵌播放入口。
- 供应商临时 `video_url` 仅用于服务端下载，不进入 Flutter、script、manifest 或日志；客户端只访问同源镜头媒体路由。
- Wan 默认关闭，只在 `XINGMU_WAN_ADAPTER_ENABLED=true` 且服务端存在 `DASHSCOPE_API_KEY` 或显式 `--dashscope-key-file` 时可用；可选 `BAILIAN_WORKSPACE_ID`。未读取、输出或提交任何 Key，不默认搜索 D 盘。
- 当前验证：Dart format 44 files、0 changed，Flutter analyze 0 issues，Flutter 142/142 `passed`，Python 30/30 `passed`；Android APK 157,402,618 bytes（SHA-256 `09677D2940743DCB39033C982D9C38587EFA8A56ED97F841C6170059C2AC362A`）重建 `passed`；OpenHarmony x64 unsigned HAP 103,749,706 bytes（SHA-256 `4EB4E6709B8961B17B83E91CC7BB36EFA9A5E80D820A9E63B6D317E3BEB6A807`）重建 `passed`。
- Video Lab OpenAPI 0.3.0 YAML/本地引用/路径参数/操作 ID/后端实例检查：`passed`，15 paths、15 operations、92 refs、0 unresolved，14 个 local/hybrid 构造器形状实例通过 JSON Schema 2020-12；独立 OpenAPI 语义 linter：`not run`。
- Android/OpenHarmony 模拟器 E2E：`not run`；Wan 真实付费请求、实际费用与视频效果：`not run`；千问脚本、Wan 图片重绘和 CosyVoice Adapter 仍未实现。
- Windows 上 CPF/Hvigor 只接受原生插件相对 `srcPath`；项目在 C 盘、Pub 缓存在 D 盘时 HAP 首轮构建 `failed`。切换到同盘 C 盘用户级 Pub 缓存后重建 `passed`；未修改全局 Git 配置。
- Android API 35 最新包的本地播放器 smoke：`passed`。本地三镜头模板任务完成后，“播放 MP4”进入内嵌页，识别时长 `0:09` 并实际播放至 `0:09 / 0:09`；目标日志无 Flutter/FATAL/网络播放错误。该结果不等于 Wan 图生视频效果或 OpenHarmony 播放 E2E。
