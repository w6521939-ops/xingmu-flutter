# SESSION-2026-08-20-1504

## 任务分类

项目启动配置、Flutter 客户端、跨平台网络、API 契约、UI 验收、安全与发布边界。

## 目标

为星幕 AI 漫剧 Flutter 客户端配置 Android/OpenHarmony 平台基线，并建立可供后端实现的契约、MVP、架构、安全、许可证和构建说明。

## 修改范围

- Android 主/debug manifest 与应用显示名。
- OpenHarmony API 20、OpenHarmony runtime、default 设备类型、联网权限与应用文案。
- Flutter 领域、数据、Controller、九个页面、主题、组件与测试。
- `docs/`、`README.md`、`LICENSE`、`THIRD_PARTY_NOTICES.md`、`.gitignore` 和自有展示资源。
- `.agent/context` 与 `.agent/memory` 中文记录。

## 明确保留

- 未加入、读取或输出任何供应商密钥、签名和用户素材。
- 未跟踪 `android/local.properties`、`ohos/local.properties` 或签名材料。
- 原始 Windows/Electron 项目目录未被修改；公开仓库为独立 Flutter 实现。

## 关键结果

- Android main 增加 INTERNET；应用名为“星幕 AI 漫剧”。
- Android cleartext 只存在于 debug manifest，main/profile 未放行。
- OpenHarmony 配置为 compile/target/compatible API 20、`runtimeOS: OpenHarmony`、`deviceTypes: default` 和 INTERNET 权限。
- OpenAPI 3.1 契约包含 23 个路径，覆盖项目、剧本任务、资产、生成计划/运行/子任务、SSE、预算与导出。
- README 明确演示模式、本地后端地址、生产 HTTPS、可信后端和无手机端模型推理边界。
- Apache-2.0 许可证与第三方参考/接口边界已记录，不复制参考项目代码。
- image2 概念稿落地为深色电影风 Flutter 首页；390×844 截图与整页热力图报告已纳入文档。

## 验证

- `passed`：CPF-Flutter `flutter --version`，版本 `3.35.8-ohos-1.0.3-beta`、Dart 3.9.2、CPF GitCode fork。
- `passed`：OpenAPI 3.1 UTF-8 YAML、23 个路径、40 个 schema、208 个本地引用和路径参数一致性检查；未安装 OpenAPI 语义 linter。
- `passed`：Android main/debug/profile manifest XML 解析。
- `passed`：静态检查 `usesCleartextTraffic` 只存在于 Android debug manifest。
- `passed`：`git check-ignore` 确认 Android/OpenHarmony `local.properties` 被忽略。
- `passed`：`hdc list targets -v` 返回 COM3、COM4；这只证明 HDC 可枚举，不代表应用运行验证。
- `passed`：`flutter analyze`，0 issues。
- `passed`：`flutter test --reporter expanded`，113 tests。
- `passed`：`flutter build apk --debug --dart-define=DEMO_MODE=true`，产出 149.07 MiB Android debug APK。
- `passed`：Hvigor `assembleHap --mode module -p module=entry@default`，API 20 编译打包成功并产出 131.98 MiB unsigned HAP；CPF 依赖仍有 ArkTS/deprecation warnings。
- `failed`：`flutter build hap --debug --dart-define=DEMO_MODE=true` 的签名阶段；本机未配置 DevEco 调试签名，业务编译已通过，未向仓库写入证书或密码。
- `not run`：Android/OpenHarmony 真机或模拟器安装与运行。

## 剩余风险

- OpenAPI 已完成 YAML、本地引用、路径参数和关键恢复字段检查；仍需 OpenAPI 3.1 语义 linter 与真实后端契约测试。
- OpenHarmony API 20 已通过 Hvigor 编译；可安装 signed HAP 仍需开发者在本机 DevEco Studio 配置调试签名。
- release 签名、HTTPS 后端、真实 SSE 和真实模型生成均不在本次客户端平台配置中验证。
- 本机 debug APK 与 unsigned HAP 包含 Dart debug kernel 的本机源码路径，只作编译证据，不作公开交付件。
- create/adopt/start/retry 的歧义意图仅在进程内保存，进程死亡后的安全重放需要持久化 journal；12 秒网络限制不是整请求总 deadline。
